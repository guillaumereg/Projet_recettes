angular.module('utilisateurServices', [])

	.factory('Utilisateur', function($http) {   //user = nom de notre factory

		var userFactory = {}; // Create the User object

		return userFactory;
	});
